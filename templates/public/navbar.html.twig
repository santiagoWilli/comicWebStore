<nav class="navbar navbar-expand-sm sticky-top" style="background-color: black;">
    <ul class="navbar-nav w-100 d-flex justify-content-between align-items-center">
        <li class="nav-item" style="width:33%;">
            <a class="btn btn-outline-warning mr-1" href="">Inicio</a>
            <a class="btn btn-outline-warning mr-1" href="">Cómics</a>
            {% if app.user %}
                <span>Hola, {{ app.user.name }} </span>
            {% else %}

            {% endif %}
        </li>
        <li class="nav-item">
            <h1 class="align-center mr-3">COMIC WEB STORE</h1>
        </li>
        <li class="nav-item" style="width:33%;">
            <div class="row" style="flex-wrap: inherit;">
                {% if app.user %}
                    <a class='btn btn-outline-warning mr-1 ml-auto' href=''>Cerrar sesión</a>
                {% else %}
                    <a class='btn btn-outline-warning mr-1 ml-auto' href=''>Iniciar sesión</a>
                {% endif %}
            </div>
        </li>
    </ul>
</nav>