<nav class="navbar navbar-expand-sm sticky-top mb-3" style="background-color: black;">
    <ul class="navbar-nav w-100 d-flex justify-content-between align-items-center">
        <li class="nav-item" style="width:33%;">
            <a class="btn btn-outline-warning mr-1" href="">Inicio</a>
            <a class="btn btn-outline-warning mr-1" href="">Cómics</a>
        </li>
        <li class="nav-item">
            <h1 class="align-center mr-3">COMIC WEB STORE</h1>
        </li>
        <li class="nav-item" style="width:33%;">
            <div class="row" style="flex-wrap: inherit;">
                {% if app.user %}
                    <span>Hola, {{ app.user.name }} </span>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class='btn btn-outline-primary mr-1 ml-auto' href='{{ path('admin_index') }}'>Panel de admin.</a>
                    {% endif %}
                    <a class='btn btn-outline-warning mr-1 ml-auto' href='{{ path('app_logout') }}'>Cerrar sesión</a>
                {% else %}
                    <a class='btn btn-outline-warning mr-1 ml-auto' href='{{ path('app_login') }}'>Iniciar sesión</a>
                {% endif %}
            </div>
        </li>
    </ul>
</nav>
